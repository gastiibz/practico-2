base= float(input("ingresa la base del area en metros:"))

Altura = float(input("Ingresa la altura del area en metros:"))

area= 0.5 * base * Altura

print(f"El area de la superficie es: {area} metros cuadrados")
